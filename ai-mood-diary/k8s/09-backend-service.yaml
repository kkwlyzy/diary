apiVersion: v1
kind: Service
metadata:
  name: mood-diary-backend
  namespace: ai-mood-diary
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: mood-diary-backend