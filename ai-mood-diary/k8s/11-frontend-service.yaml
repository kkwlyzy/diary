apiVersion: v1
kind: Service
metadata:
  name: mood-diary-frontend
  namespace: ai-mood-diary
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: mood-diary-frontend